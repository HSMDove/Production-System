"أنت الآن كبير مهندسي الأنظمة والأمن السيبراني (Senior Full-Stack & Security Architect). سنقوم ببناء 'لوحة تحكم الإدارة' (Admin Dashboard) شاملة لمنصة 'نَسَق' مع نظام إعلانات ذكي.

المطلوب تنفيذه بدقة وتفصيل:

أولاً: تجهيز قاعدة البيانات (Neon DB - Additive Only):

أضف الحقول والجداول اللازمة لتمييز الإدارة، وتتبع الإعلانات للمستخدمين، دون حذف أو إتلاف أي بيانات سابقة.

حساب hylf.111@gmail.com هو المدير الأساسي (Super Admin). عند التشغيل لأول مرة، ولّد كلمة سر عشوائية لهذا الحساب واطبعها في (Console) لمرة واحدة فقط للوصول الأولي.

ثانياً: تدفق الدخول وحماية الإدارة (Admin Auth Flow):

دخول المستخدم العادي يبقى عبر رمز البريد.

إذا كان الإيميل لـ 'مدير'، يطلب النظام 'كلمة سر الإدارة' كطبقة حماية ثانية بعد إدخال الرمز.

بعد الدخول، تظهر 'شاشة عبور' (Gateway Screen) تخير المدير بين: 'الدخول كمستخدم' أو 'الدخول للوحة التحكم'.

ثالثاً: تصميم لوحة التحكم (Admin Dashboard Modules):

1. قسم الإحصائيات (Analytics):

إجمالي عدد المستخدمين.

مقارنة: عدد مستخدمي 'API الموقع الافتراضي' مقابل 'API مستقل'.

2. إعدادات النظام (Global Settings):

تعديل (اسم الموقع) و (اسم المساعد الذكي - فكري).

تعديل/حذف 'مفتاح API البحث الافتراضي'.

[جديد] حقل لتعديل 'رقم الإصدار' (Version Number) والذي ينعكس في إعدادات المستخدمين.

3. نظام الإعلانات والتوجيه (Announcement & Onboarding System):

إعلانات البطاقات (Modal Cards): واجهة لإنشاء إعلان يظهر كبطاقة منبثقة (عنوان، نص، صورة/أيقونة) عند فتح المستخدم للموقع. مع تحكم كامل: (تفعيل/تعطيل)، وتحديد (كم مرة يظهر للمستخدم قبل أن يختفي للأبد، مثلاً مرة أو مرتين). يجب تتبع مشاهدات كل مستخدم في قاعدة البيانات.

الشريط الإعلاني العلوي (Top Banner): شريط يظهر تحت القائمة الرئيسية. يحتوي على نص ورابط، مع زر تفعيل/تعطيل من اللوحة.

4. إدارة المدراء (Admins Management):

إضافة مدير جديد عبر الإيميل (توليد كلمة سر عشوائية تظهر لمرة واحدة للـ Super Admin).

إعادة تعيين كلمة سر أي مدير. وإلغاء صلاحيات المدراء (بدون حذف حساباتهم كمستخدمين).

رابعاً: لمسات الخبير (Expert Additions):
بصفتك خبيراً في بناء أنظمة الـ SaaS، أضف 2 إلى 3 ميزات ضرورية في لوحة التحكم لم أذكرها (مثل: سجل نشاطات الإدارة Audit Logs، أو حالة سيرفرات الـ API)، بشرط أن تكون مفيدة ولا تكسر المنطق الحالي للمشروع.

القيود الصارمة (Strict Constraints):

لا توجد خاصية لحذف المستخدمين أبداً.

ركز على أمان الجلسات (Sessions) وحماية روابط الإدارة.

استخدم أفضل المعماريات البرمجية لتحقيق هذا النظام المتكامل بكفاءة عالية."