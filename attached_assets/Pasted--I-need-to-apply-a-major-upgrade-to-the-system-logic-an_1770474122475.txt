"I need to apply a major upgrade to the system logic and UI. The current foundation is great, but we need to move from 'Global Settings' to 'Granular Control' per folder, and fix a critical bug.

1. CRITICAL BUG FIX: AI Persona Application

The Issue: The 'AI Style/System Prompt' I saved in Settings works in the 'Test' button, but it is IGNORED during the actual news fetching pipeline. The system is currently using a default prompt.

The Fix: You must update the background job logic. Before rewriting any article, fetch the system_prompt from the settings table and pass it to the AI client. The actual news MUST reflect the persona defined in Settings.

2. Feature: Per-Folder Automation & Configuration Currently, I have to open a folder to refresh it. I need a 'Dashboard Control Center' on the main page.

Database Changes: Update the feed_folders table to include:

is_background_active (Boolean): To turn auto-refresh on/off for this specific folder.

refresh_interval (Integer): Specific time in minutes (e.g., 30, 60, 120) for this folder.

notify_slack (Boolean): Override global setting.

notify_telegram (Boolean): Override global setting.

UI Changes (Main Dashboard):

On each Folder Card, add a control bar with:

A Toggle Switch (On/Off) for background fetching.

A Dropdown for Interval (15m, 30m, 1h, etc.).

Small Icons for Slack/Telegram that I can toggle on/off directly from the card (to mute specific folders).

Backend Logic: The scheduler loop must now check each folder's specific interval and active status independently, running in the background even if I'm not inside the folder.

3. Feature: Split-View Mode (Multi-Folder)

I want to monitor two folders at once.

Add a 'Split View' button on the dashboard.

When clicked, allow me to select two folders, and render them side-by-side (50% width each) on the screen, so I can compare news streams in real-time.

Implementation Plan:

Fix the AI Persona bug first.

Update the Schema for feed_folders.

Refactor the Scheduler to handle per-folder logic.

Update the Dashboard UI with the new controls and Split View.

Proceed."