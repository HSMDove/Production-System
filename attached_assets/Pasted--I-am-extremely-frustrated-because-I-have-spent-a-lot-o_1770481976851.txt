"I am extremely frustrated because I have spent a lot of money and months of development, yet the core features are still buggy. This is a critical repair request. Do NOT break the existing working RSS/Website fetching.

Here are the strict requirements to finalize this project:

1. CRITICAL: Enforce AI Persona from Settings The system is currently ignoring my custom prompt. You MUST ensure that every single article fetched is rewritten using the EXACT text found in the Settings page under the field labeled: « أسلوب Tech Voice حدد شخصية الكتابة - كيف يعيد الذكاء الاصطناعي صياغة الأخبار »

Action: In the backend rewrite_article function, query the database for this specific string before sending the request to the AI.

2. Background Automation Logic

True Background Fetch: The auto-refresh logic must run on the server side (cron job/scheduler) independent of the frontend. It must fetch news for a folder even if I do not have that folder open in my browser.

Logic: When the timer triggers, it must execute the exact same function used by the manual 'Refresh' button.

3. FIX: YouTube Integration (Major Issues)

The Bug: Currently, it fetches videos that are 2-3 months old.

The Fix: You must force the fetch order to be by date/newest. If you are using standard RSS, ensure the URL parameter ?order=date is applied or use a better scraping method to get the latest videos only.

The "View All" Bug: When I have multiple YouTube channels in one folder and click "All", it shows nothing. It only works if I click a specific channel. Fix the SQL query for the 'All' view to correctly aggregate items from all sources in that folder.

4. FIX: X (Twitter) Integration

The Bug: Adding an X account returns zero results.

The Fix: Twitter's standard RSS is dead. You must implement a working alternative (e.g., using a Nitter bridge instance, a scraping library like ntscraper, or a RapidAPI endpoint). Do not just 'try' standard RSS again; it won't work. I need to see tweets.

5. NEW: TikTok Integration

Requirement: I need to add TikTok accounts as sources just like YouTube/RSS.

Action: Implement a fetcher for TikTok (likely via a scraping library or a third-party RSS bridge) that grabs the latest videos/captions.

Summary:

DO NOT touch the working Website/RSS fetching.

Focus 100% on fixing the AI Persona application, Background Scheduler, and the Broken Social Media fetchers (YouTube/X/TikTok).

I expect a fully working system this time."